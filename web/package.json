{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@onflow/fcl": "^1.3.2",
    "flow-cadut": "^0.1.16-alpha.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "cadence-to-json": "^1.0.3"
  },
  "scripts": {
    "prestart": "mkdir -p ./src/cadut && npx flow-generate --input ../cadence --output ./src/cadut && node ./build.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@onflow/flow-cadut-generator": "^0.0.1"
  }
}
